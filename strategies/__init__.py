
"""Strategy package.

The current runtime keeps the first strategy in `core.probability` and
`core.trader` while the project is in paper-trading hardening. Future
strategy implementations should live here behind a small interface rather
than adding more branching to the orchestrator.
"""

__all__: list[str] = []
